# Examples

This folder contains synthetic or schema-only examples that do not contain participant research data. `example_metadata.csv` demonstrates the minimum metadata structure expected by the pipeline. The identifiers and filenames are fictional.
