# Local Metadata

Save the working metadata table as `metadata/pitch_metadata.csv`.

Use `../examples/example_metadata.csv` as the public schema. Do not commit participant-identifying metadata to the public repository.
